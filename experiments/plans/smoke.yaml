# Smoke test: exercise the full method ladder on the smallest dataset, one
# measure, one error bound, a single repetition. Cheap end-to-end check that the
# run -> validate pipeline works before launching the large plans. Everything
# else inherits from _defaults.yaml.
plan_id: smoke
workloads:
  - synth10_1M_clustered
nm: [1]
eb: [0.05]
run_id: [0]
